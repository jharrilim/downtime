import React from 'react';
import { FormControl, Paper, Typography } from '@material-ui/core';

export const NewPostForm = () => (
  <Paper>
    <Typography draggable component="h2">New Post</Typography>
  </Paper>
);
